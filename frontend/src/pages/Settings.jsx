import React from 'react'
import Layout from './Layout';
import Settings from '../components/settings/SettingsContent';

export default function Log() {

    return (
        <Layout>
            <Settings />
        </Layout>
    )
}
